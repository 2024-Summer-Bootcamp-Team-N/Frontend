services:
  frontend:
    container_name: frontend
    env_file:
      - .env
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - 5173:5173
    volumes:
      - .:/frontend/
      - /frontend/node_modules
    networks:
      - nteam
    tty: true
    stdin_open: true

networks:
  nteam:
    driver: bridge